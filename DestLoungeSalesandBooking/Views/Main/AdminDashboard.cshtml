@{
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

@Html.AntiForgeryToken()

<div ng-app="DestLoungeSalesandBooking" ng-controller="DestLoungeSalesandBookingController">

    <!-- Sidebar Navigation -->
    <aside class="sidebar">
        <div class="logo-container">
            <a href="@Url.Action("AdminDashboard","Main")">
                <img src="~/Content/Pictures/DestLogo.png" class="sidebar-logo" alt="D'est Lounge">
            </a>
        </div>
        <nav class="sidebar-nav">
            <a href="@Url.Action("AdminDashboard","Main")" class="nav-item active">DASHBOARD</a>
            <a href="@Url.Action("AdminNailDesk","Main")" class="nav-item">NAIL DESK</a>
            <a href="@Url.Action("AdminSalesPage","Main")" class="nav-item">SALES</a>
        </nav>
        <div class="sidebar-footer">
            <a href="@Url.Action("Logout","Account")" class="admin-link" title="Logout">
                <i class="fa-solid fa-right-from-bracket"></i>
                <span>Logout</span>
            </a>
        </div>
    </aside>

    <!-- Mobile Top Bar -->
    <div class="mobile-topbar">
        <a href="@Url.Action("AdminDashboard","Main")">
            <img src="~/Content/Pictures/DestLogo.png" alt="D'est Lounge">
        </a>
        <button class="mobile-menu-btn" onclick="toggleMobileNav()">
            <i class="fa-solid fa-bars"></i>
        </button>
    </div>

    <div class="mobile-nav-overlay" id="mobileOverlay" onclick="toggleMobileNav()"></div>

    <div class="mobile-nav-drawer" id="mobileDrawer">
        <nav class="sidebar-nav">
            <a href="@Url.Action("AdminDashboard","Main")" class="nav-item active">DASHBOARD</a>
            <a href="@Url.Action("AdminNailDesk","Main")" class="nav-item">NAIL DESK</a>
            <a href="@Url.Action("AdminSalesPage","Main")" class="nav-item">SALES</a>
        </nav>
        <div class="sidebar-footer">
            <a href="@Url.Action("Logout","Account")" class="admin-link" title="Logout">
                <i class="fa-solid fa-right-from-bracket"></i>
                <span>Logout</span>
            </a>
        </div>
    </div>

    <script>
        window.isAdminPage = true;
    </script>

    <!-- Main Content -->
    <div class="main-content">

        <div class="dashboard-header">
            <h1 class="dashboard-title">DASHBOARD</h1>
        </div>

        <!-- ═══════════════════════════════════════════════════ -->
        <!-- ANALYTICS -->
        <!-- ═══════════════════════════════════════════════════ -->
        <div class="tab-content">
            <h2 class="section-title">Salon's Analytics</h2>

            <div class="analytics-top-row">
                <div class="analytics-chart-card">
                    <div class="chart-header">
                        <span class="chart-title">Sales Overview</span>
                        <select class="admin-select" ng-model="analyticsFilter" ng-change="loadAnalytics()">
                            <option value="today">Today</option>
                            <option value="week">This Week</option>
                            <option value="month">This Month</option>
                            <option value="year">This Year</option>
                        </select>
                    </div>
                    <div class="analytics-summary-row">
                        <div class="analytics-kpi">
                            <span class="kpi-label">Total Revenue</span>
                            <span class="kpi-value">₱{{analytics.totalRevenue | number:2}}</span>
                        </div>
                        <div class="analytics-kpi">
                            <span class="kpi-label">Completed Bookings</span>
                            <span class="kpi-value">{{analytics.completedBookings}}</span>
                        </div>
                        <div class="analytics-kpi">
                            <span class="kpi-label">Top Service</span>
                            <span class="kpi-value kpi-value-sm">{{analytics.topService || '—'}}</span>
                        </div>
                    </div>
                    <canvas id="analyticsChart" height="160"></canvas>
                </div>
                <div class="analytics-stats-col">
                    <div class="stat-card">
                        <div class="stat-icon"><i class="fa-solid fa-calendar-check"></i></div>
                        <div class="stat-info">
                            <span class="stat-label">Active Pending Approvals</span>
                            <span class="stat-num">{{pendingBookingsCount || 0}}</span>
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon stat-icon-star"><i class="fa-solid fa-star"></i></div>
                        <div class="stat-info">
                            <span class="stat-label">Active Reviews</span>
                            <span class="stat-num">{{reviews.length || 0}}</span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="analytics-section-card" ng-if="flaggedCount() > 0">
                <div class="analytics-section-header" style="color:#e67e22;">
                    <span><i class="fa-solid fa-flag"></i> Flagged Reviews Need Attention</span>
                    <a href="@Url.Action("AdminNailDesk","Main")" class="admin-btn admin-btn-flag admin-btn-sm">Review Now</a>
                </div>
                <div class="mini-review-list">
                    <div class="mini-review-row flagged-row" ng-repeat="review in reviews | filter:{flagged: true} | limitTo:3">
                        <span class="mini-review-text">{{review.reviewText | limitTo:60}}...</span>
                        <span style="font-size:0.75rem; color:#e67e22;">{{review.flagReason}}</span>
                    </div>
                </div>
            </div>

            <div class="analytics-section-card">
                <div class="analytics-section-header">
                    <span>Manage Active Reviews</span>
                </div>
                <div class="mini-review-list">
                    <div class="mini-review-row" ng-repeat="review in reviews | limitTo:5" ng-class="{'flagged-row': review.flagged}">
                        <span class="mini-review-text">{{review.reviewText | limitTo:40}}...</span>
                        <div class="mini-review-actions">
                            <button class="mini-btn mini-btn-flag" ng-show="!review.flagged" ng-click="openFlagModal(review)">Flag</button>
                            <button class="mini-btn mini-btn-unflag" ng-show="review.flagged" ng-click="unflagReview(review)">Unflag</button>
                            <button class="mini-btn mini-btn-archive" ng-click="archiveReview(review.reviewId)">Archive</button>
                        </div>
                    </div>
                    <div ng-if="reviews.length === 0" class="empty-mini">No active reviews.</div>
                </div>
            </div>

            <div class="analytics-section-card">
                <div class="analytics-section-header">
                    <span>Recent Bookings</span>
                    <a href="@Url.Action("AdminNailDesk","Main")" class="admin-btn admin-btn-primary admin-btn-sm">View All</a>
                </div>
                <div ng-repeat="booking in bookings | limitTo:5" class="mini-booking-card">
                    <div class="mini-booking-row">
                        <i class="fa-solid fa-user"></i>
                        <span><strong>{{booking.clientName}}</strong> — {{booking.service}}</span>
                    </div>
                    <div class="mini-booking-row">
                        <i class="fa-solid fa-calendar"></i>
                        <span>{{booking.dateTime}}</span>
                        <span class="bk-pill" ng-class="{'bk-pill-pending': booking.status === 'Pending','bk-pill-approved': booking.status === 'Approved','bk-pill-completed': booking.status === 'Completed','bk-pill-cancelled': booking.status === 'Cancelled'}">{{booking.status}}</span>
                    </div>
                </div>
                <div ng-if="bookings.length === 0" class="empty-mini">No bookings yet.</div>
            </div>
        </div>

    </div>

    <!-- MODAL: Flag Review -->
    <div class="modal-overlay" ng-show="flagModal.show === true" ng-click="closeFlagModal()">
        <div class="modal-card" ng-click="$event.stopPropagation()" style="max-width:500px;">
            <div class="modal-icon-plain" style="color:#f39c12;"><i class="fa-solid fa-flag"></i></div>
            <h3>Flag Review</h3>
            <p style="margin:10px 0 18px; color:#6b4423;">Select the reason why this review is being flagged.</p>
            <div class="field-grp" style="margin-bottom:14px;">
                <label>Reason</label>
                <select class="admin-input" ng-model="flagModal.reason">
                    <option value="">-- Select a reason --</option>
                    <option value="Inappropriate language">Inappropriate language</option>
                    <option value="Spam or irrelevant content">Spam or irrelevant content</option>
                    <option value="Offensive or abusive content">Offensive or abusive content</option>
                    <option value="False or misleading review">False or misleading review</option>
                    <option value="Contains sensitive/private information">Contains sensitive/private information</option>
                </select>
            </div>
            <div class="field-grp">
                <label>Additional note (optional)</label>
                <textarea class="admin-input" rows="3" ng-model="flagModal.note" placeholder="Add more details if needed..."></textarea>
            </div>
            <div class="modal-actions" style="margin-top:18px;">
                <button class="admin-btn admin-btn-secondary" ng-click="closeFlagModal()">Cancel</button>
                <button class="admin-btn admin-btn-flag" ng-click="submitFlagReview()" ng-disabled="!flagModal.reason"><i class="fa-solid fa-flag"></i> Confirm Flag</button>
            </div>
        </div>
    </div>

    <!-- LIGHTBOX: Review Image -->
    <div class="lightbox-overlay" ng-show="lightboxPhoto" ng-click="closeLightbox()">
        <div class="lightbox-card" ng-click="$event.stopPropagation()">
            <button class="lightbox-close" ng-click="closeLightbox()"><i class="fa-solid fa-xmark"></i></button>
            <img ng-src="{{lightboxPhoto}}" class="lightbox-img" alt="Review photo">
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <div class="footer-container">
            <h2 class="footer-title">Follow our Socials!</h2>
            <div class="social-links">
                <a href="https://www.tiktok.com/&#64;destlounge" class="social-item" target="_blank"><i class="fa-brands fa-tiktok"></i><span>destlounge</span></a>
                <a href="https://www.facebook.com/profile.php?id=61578352581708" class="social-item" target="_blank"><i class="fa-brands fa-facebook"></i><span>D'est Lounge</span></a>
                <a href="https://www.instagram.com/destlounge/" class="social-item" target="_blank"><i class="fa-brands fa-instagram"></i><span>destlounge</span></a>
            </div>
        </div>
    </footer>
</div>

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Georgia', serif;
        background-color: #d4c4b0;
    }

    .sidebar {
        position: fixed;
        left: 0;
        top: 0;
        width: 10%;
        min-width: 9rem;
        height: 100vh;
        background-color: #e7dfd6;
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 1.25rem 0;
        z-index: 1000;
    }

    .logo-container {
        margin-bottom: 2rem;
    }

    .sidebar-logo {
        width: 3.5rem;
        height: auto;
    }

    .sidebar-nav {
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 0.3rem;
        width: 100%;
    }

    .nav-item {
        color: #5a4a3a;
        text-decoration: none;
        padding: 1rem 0.625rem;
        font-size: 1rem;
        font-weight: 500;
        text-align: center;
        transition: background-color 0.3s;
        letter-spacing: 0.0625rem;
        display: block;
    }

        .nav-item:hover {
            background-color: rgba(90,74,58,0.1);
        }

        .nav-item.active {
            background-color: rgba(139,111,71,0.3);
            border-left: 0.25rem solid #8b6f47;
        }

    .sidebar-footer {
        margin-top: auto;
        padding: 1.25rem 0;
    }

    .admin-link {
        color: #5a4a3a;
        text-decoration: none;
        font-size: 0.75rem;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.3125rem;
    }

        .admin-link i {
            font-size: 1.25rem;
        }

    .main-content {
        margin-left: 10%;
        min-height: 100vh;
        background-color: #f5f0eb;
        display: flex;
        flex-direction: column;
    }

    .dashboard-header {
        background-color: #e7dfd6;
        padding: 1.5rem 2.5rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-bottom: 3px solid #c1b2a5;
    }

    .dashboard-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: #6b4423;
        letter-spacing: 0.1em;
        margin: 0;
    }

    .tab-content {
        padding: 2rem 2.5rem;
        flex: 1;
    }

    .section-title {
        font-size: 2rem;
        font-weight: 600;
        color: #6b4423;
        margin: 0 0 1.5rem 0;
    }

    .analytics-top-row {
        display: flex;
        gap: 1.5rem;
        margin-bottom: 2rem;
        flex-wrap: wrap;
    }

    .analytics-chart-card {
        flex: 2;
        min-width: 280px;
        background: white;
        border-radius: 1rem;
        border: 2px solid #d4c4b0;
        padding: 1.5rem;
        box-shadow: 2px 4px 12px rgba(0,0,0,0.07);
    }

    .chart-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
    }

    .chart-title {
        font-size: 1rem;
        font-weight: 600;
        color: #6b4423;
    }

    .analytics-stats-col {
        flex: 1;
        min-width: 200px;
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .stat-card {
        background: white;
        border-radius: 1rem;
        border: 2px solid #d4c4b0;
        padding: 1.5rem;
        display: flex;
        align-items: center;
        gap: 1rem;
        box-shadow: 2px 4px 12px rgba(0,0,0,0.07);
        flex: 1;
    }

    .stat-icon {
        width: 3rem;
        height: 3rem;
        border-radius: 50%;
        background: rgba(139,111,71,0.15);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #6b4423;
        font-size: 1.3rem;
        flex-shrink: 0;
    }

    .stat-icon-star {
        background: rgba(212,168,67,0.15);
        color: #d4a843;
    }

    .stat-info {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
    }

    .stat-label {
        font-size: 0.85rem;
        color: #8b6f47;
        font-weight: 600;
    }

    .stat-num {
        font-size: 2.5rem;
        font-weight: 700;
        color: #6b4423;
        line-height: 1;
    }

    .analytics-section-card {
        background: white;
        border-radius: 1rem;
        border: 2px solid #d4c4b0;
        padding: 1.25rem 1.5rem;
        margin-bottom: 1.5rem;
        box-shadow: 2px 4px 8px rgba(0,0,0,0.06);
    }

    .analytics-section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
        font-size: 1rem;
        font-weight: 600;
        color: #6b4423;
        border-bottom: 1px solid #e7dfd6;
        padding-bottom: 0.75rem;
    }

    .analytics-summary-row {
        display: flex;
        gap: 1rem;
        margin-bottom: 1rem;
        flex-wrap: wrap;
    }

    .analytics-kpi {
        flex: 1;
        background: #faf7f4;
        border: 1.5px solid #e7dfd6;
        border-radius: 0.75rem;
        padding: 0.75rem 1rem;
        display: flex;
        flex-direction: column;
        gap: 0.2rem;
        min-width: 100px;
    }

    .kpi-label {
        font-size: 0.72rem;
        font-weight: 700;
        color: #8b6f47;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .kpi-value {
        font-size: 1.5rem;
        font-weight: 700;
        color: #6b4423;
        line-height: 1.1;
    }

    .kpi-value-sm {
        font-size: 0.95rem;
        font-weight: 600;
    }

    .mini-btn {
        padding: 0.3rem 0.7rem;
        border: none;
        border-radius: 0.75rem;
        font-size: 0.78rem;
        font-weight: 600;
        cursor: pointer;
        font-family: 'Georgia', serif;
        transition: all 0.2s;
    }

    .mini-btn-flag {
        background: #f39c12;
        color: white;
    }

    .mini-btn-unflag {
        background: #e7dfd6;
        color: #5d4037;
    }

    .mini-btn-archive {
        background: #7f8c8d;
        color: white;
    }

    .mini-review-list {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .mini-review-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0.6rem 0.75rem;
        background: #faf7f4;
        border-radius: 0.5rem;
        border: 1px solid #e7dfd6;
    }

    .mini-review-text {
        font-size: 0.85rem;
        color: #5a4a3a;
        flex: 1;
        min-width: 0;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
        font-style: italic;
    }

    .mini-review-actions {
        display: flex;
        gap: 0.4rem;
        flex-shrink: 0;
    }

    .flagged-row {
        border-color: #f4c6b0;
        background: #fff8f4;
    }

    .mini-booking-card {
        background: rgba(232,223,213,0.7);
        border-radius: 1rem;
        padding: 1rem 1.25rem;
        margin-bottom: 0.75rem;
        border: 1px solid #d4c4b0;
    }

    .mini-booking-row {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.35rem 0;
        font-size: 0.88rem;
        color: #5a4a3a;
    }

        .mini-booking-row i {
            width: 1rem;
            color: #8b6f47;
        }

    .empty-mini {
        text-align: center;
        color: #a08060;
        font-style: italic;
        padding: 1rem;
        font-size: 0.88rem;
    }

    .bk-pill {
        display: inline-flex;
        align-items: center;
        padding: .25rem .75rem;
        border-radius: 20px;
        font-size: .72rem;
        font-weight: 700;
    }

    .bk-pill-pending {
        background: #fff3e0;
        color: #e67e22;
    }

    .bk-pill-approved {
        background: #e8f5e9;
        color: #2e7d32;
    }

    .bk-pill-completed {
        background: #e3f2fd;
        color: #1565c0;
    }

    .bk-pill-cancelled {
        background: #fce4ec;
        color: #c62828;
    }

    .admin-btn {
        padding: 0.55rem 1.2rem;
        font-size: 0.88rem;
        font-weight: 600;
        border: none;
        border-radius: 20px;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        transition: all 0.2s;
        font-family: 'Georgia', serif;
        text-decoration: none;
    }

        .admin-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

    .admin-btn-sm {
        padding: 0.4rem 0.9rem;
        font-size: 0.8rem;
    }

    .admin-btn-primary {
        background-color: #6b4423;
        color: white;
    }

        .admin-btn-primary:hover:not(:disabled) {
            background-color: #5d3a1f;
        }

    .admin-btn-flag {
        background-color: #f39c12;
        color: white;
    }

        .admin-btn-flag:hover {
            background-color: #d68910;
        }

    .admin-btn-secondary {
        background-color: #e7dfd6;
        color: #5d4037;
    }

        .admin-btn-secondary:hover {
            background-color: #d4c4b0;
        }

    .admin-input {
        padding: 0.65rem 1rem;
        border: 2px solid #d4c4b0;
        border-radius: 10px;
        font-size: 0.93rem;
        color: #3d2817;
        font-family: inherit;
        outline: none;
        width: 100%;
        box-sizing: border-box;
        transition: border-color 0.3s;
    }

        .admin-input:focus {
            border-color: #6b4423;
        }

    .admin-select {
        padding: 0.6rem 1rem;
        border: 2px solid #d4c4b0;
        border-radius: 20px;
        font-size: 0.9rem;
        color: #3d2817;
        font-family: inherit;
        background-color: white;
        cursor: pointer;
        outline: none;
    }

    .modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(30,15,5,0.6);
        z-index: 9999;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
    }

    .modal-card {
        background-color: white;
        border-radius: 16px;
        padding: 2.5rem;
        max-width: 420px;
        width: 100%;
        text-align: center;
        box-shadow: 0 16px 48px rgba(0,0,0,0.25);
    }

    .modal-icon-plain {
        font-size: 3rem;
        color: #e67e22;
        margin-bottom: 1rem;
    }

    .modal-card h3 {
        font-size: 1.4rem;
        color: #3d2817;
        margin: 0 0 0.8rem;
    }

    .modal-card p {
        font-size: 0.95rem;
        color: #5d4037;
        line-height: 1.6;
        margin: 0 0 1.8rem;
    }

    .modal-actions {
        display: flex;
        justify-content: center;
        gap: 1rem;
    }

    .field-grp {
        display: flex;
        flex-direction: column;
        gap: 0.35rem;
        flex: 1;
    }

    .lightbox-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.75);
        z-index: 9999;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
        box-sizing: border-box;
    }

    .lightbox-card {
        position: relative;
        max-width: 90vw;
        max-height: 90vh;
        background-color: #f5f0eb;
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 20px 60px rgba(0,0,0,0.5);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
    }

    .lightbox-img {
        max-width: 100%;
        max-height: 80vh;
        width: auto;
        height: auto;
        object-fit: contain;
        display: block;
        border-radius: 12px;
    }

    .lightbox-close {
        position: absolute;
        top: 10px;
        right: 10px;
        background: rgba(61,40,23,0.85);
        color: #fff;
        border: none;
        border-radius: 50%;
        width: 38px;
        height: 38px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 2;
        font-size: 1rem;
    }

    footer {
        background-color: #e7d7c8;
        border: 2px solid #d4c4b0;
        border-left: none;
        border-right: none;
        padding: 2.5rem 3rem;
        text-align: center;
    }

    .footer-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: #6b4423;
        margin: 0 0 2rem;
        font-style: italic;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    .social-links {
        display: flex;
        justify-content: center;
        gap: 5rem;
    }

    .social-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        text-decoration: none;
        color: #5a4a3a;
        transition: transform 0.3s;
    }

        .social-item:hover {
            transform: translateY(-0.1875rem);
        }

        .social-item i {
            font-size: 2.25rem;
        }

        .social-item span {
            font-size: 1rem;
            font-weight: 500;
        }

    .mobile-topbar {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        background-color: #e7dfd6;
        z-index: 1100;
        padding: 0.75rem 1rem;
        align-items: center;
        justify-content: space-between;
        border-bottom: 2px solid #c1b2a5;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

        .mobile-topbar img {
            height: 2.5rem;
            width: auto;
        }

    .mobile-menu-btn {
        background: none;
        border: none;
        font-size: 1.5rem;
        color: #5a4a3a;
        cursor: pointer;
    }

    .mobile-nav-drawer {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 75%;
        max-width: 16rem;
        height: 100vh;
        background-color: #e7dfd6;
        z-index: 1200;
        flex-direction: column;
        padding: 1.5rem 0;
        box-shadow: 4px 0 12px rgba(0,0,0,0.15);
        transform: translateX(-100%);
        transition: transform 0.3s ease;
    }

        .mobile-nav-drawer.open {
            transform: translateX(0);
        }

        .mobile-nav-drawer .nav-item {
            font-size: 1rem;
            padding: 1rem 1.5rem;
            text-align: left;
            color: #5a4a3a;
        }

        .mobile-nav-drawer .sidebar-footer {
            margin-top: auto;
            padding: 1rem 1.5rem;
        }

    .mobile-nav-overlay {
        display: none;
        position: fixed;
        inset: 0;
        background: rgba(0,0,0,0.4);
        z-index: 1150;
    }

        .mobile-nav-overlay.open {
            display: block;
        }

    @@media (max-width: 64rem) {
        .sidebar {
            width: 5.5rem;
            min-width: unset;
        }

        .main-content {
            margin-left: 5.5rem;
        }

        .nav-item {
            font-size: 0.6rem;
            padding: 0.75rem 0.25rem;
            letter-spacing: 0;
        }

        .sidebar-logo {
            width: 2.5rem;
        }

        .admin-link span {
            font-size: 0.6rem;
        }

        .dashboard-title {
            font-size: 1.8rem;
        }

        .analytics-top-row {
            flex-direction: column;
        }
    }

    @@media (max-width: 48rem) {
        .sidebar {
            width: 5rem;
            min-width: unset;
        }

        .main-content {
            margin-left: 5rem;
        }

        .nav-item {
            font-size: 0.55rem;
            padding: 0.65rem 0.2rem;
        }

        .sidebar-logo {
            width: 2rem;
        }

        .tab-content {
            padding: 1.25rem 1rem;
        }

        .social-links {
            gap: 1.5rem;
        }
    }

    @@media (max-width: 30rem) {
        .sidebar {
            display: none;
        }

        .main-content {
            margin-left: 0;
            padding-top: 4.5rem;
        }

        .mobile-topbar {
            display: flex !important;
        }

        .mobile-nav-drawer {
            display: flex;
        }

        .tab-content {
            padding: 1rem;
        }

        .social-links {
            gap: 1rem;
        }

        .footer-title {
            font-size: 1.5rem;
        }
    }
</style>

<script>
    function toggleMobileNav() {
        var drawer = document.getElementById('mobileDrawer');
        var overlay = document.getElementById('mobileOverlay');
        drawer.classList.toggle('open');
        overlay.classList.toggle('open');
    }
</script>

@section scripts {
    <script>
        angular.element(document).ready(function () {
            var scope = angular.element(document.querySelector('[ng-controller]')).scope();
            if (scope) {
                scope.$apply(function () {
                    scope.loadBookings();
                    scope.loadReviews();
                    scope.loadArchivedReviews();
                    scope.loadAnalytics();
                });
            }
        });
    </script>
}